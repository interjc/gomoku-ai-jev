{
  "name": "gomoku-ai-jev",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "description": "Gomoku game. Jev chooses moves, with minimax as the fallback.",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "dev": "astro dev",
    "build": "astro build",
    "preview": "astro preview",
    "deploy": "astro build && wrangler deploy",
    "test": "node --test tests/*.test.js",
    "astro": "astro"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^14.3.0",
    "@typesafe-ai/sdk": "^0.6.0",
    "astro": "^7.3.1"
  },
  "devDependencies": {
    "wrangler": "^4.129.0"
  },
  "license": "MIT"
}
